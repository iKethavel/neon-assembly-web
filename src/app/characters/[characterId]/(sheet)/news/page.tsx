import { NewsLog } from "./_lib/components/NewsLog";

// eslint-disable-next-line @typescript-eslint/require-await
export default async function NewsPage() {

  return (
    <div>
      <NewsLog />
    </div>
  );
};
